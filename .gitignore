# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — clé API IA (proxy local Claude vision)
.env
.env.*
!.env.example

# Images temporaires envoyées au CLI Claude Code (proxy IA)
.ai-tmp/

# Asset produit © Adobe Inc. — hors dépôt, récupéré via scripts/fetch-asset.ps1
/public/asset/

# NB: la police Adobe Clean (public/fonts/*.otf, © Adobe Inc.) est volontairement
# COMMITÉE dans ce dépôt privé (choix utilisateur). scripts/fetch-fonts.ps1 reste
# dispo comme fallback (récup depuis OneDrive).

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
